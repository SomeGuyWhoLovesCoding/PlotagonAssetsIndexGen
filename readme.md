Thank you for stumbling by. I was just about to give you this:

### How this tool works

Download the file on the `Release` package, and type in whatever command prompt you have with the the file explorer and run `Main path/to/StreamingAssets`.

Then, replace your old assetsIndex.json with the new one and that's it.

This was tested a bunch of times due to it being vibe-coded with qwen 3.7 plus but it shouldn't matter since this really is such a minitool.